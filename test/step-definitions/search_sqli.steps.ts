/**
 * SEC-003-API: SQL injection in /api/search. All step text is covered by
 * the shared `support/common-steps.ts` — this file exists so the test
 * framework has a 1:1 mapping between feature files and step files.
 */

// Intentionally empty: the search_sqli feature uses only common steps.
export {};
