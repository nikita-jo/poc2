/**
 * SEC-005 — Authentication enforcement. All step text in
 * auth_enforcement.feature is covered by the shared step defs in
 * `support/common-steps.ts` and `support/browser-steps.ts`. This
 * file exists so the test framework has a 1:1 mapping between
 * feature files and step files, which makes maintenance and
 * grep-and-replace simple.
 */

// Intentionally empty: the auth_enforcement feature uses only common steps.
export {};
