/**
 * SEC-002-API: SQL injection in /api/login. All step text is covered by
 * the shared `support/common-steps.ts` — this file exists so the test
 * framework has a 1:1 mapping between feature files and step files,
 * which makes maintenance and grep-and-replace simple.
 */

// Intentionally empty: the login_sqli feature uses only common steps.
export {};
